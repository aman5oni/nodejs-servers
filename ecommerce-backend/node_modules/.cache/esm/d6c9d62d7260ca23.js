let mongoose,validator,bcrypt,jwt,crypto,JWT_PRIVATE_KEY,JWT_EXPIRES,RESET_PASSWORD_EXPIRE;_726‍.x([["User",()=>User]]);_726‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_726‍.w("validator",[["default",["validator"],function(v){validator=v}]]);_726‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_726‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_726‍.w("crypto",[["default",["crypto"],function(v){crypto=v}]]);_726‍.w("../../config/appConfig",[["JWT_PRIVATE_KEY",["JWT_PRIVATE_KEY"],function(v){JWT_PRIVATE_KEY=v}],["JWT_EXPIRES",["JWT_EXPIRES"],function(v){JWT_EXPIRES=v}],["RESET_PASSWORD_EXPIRE",["RESET_PASSWORD_EXPIRE"],function(v){RESET_PASSWORD_EXPIRE=v}]]);






const userSchema = new mongoose.Schema({
  name: {
    type: String,
    required: [true, "Please Enter Your Name"],
    maxLength: [30, "Name Cannot Exceed 30 Charecters"],
    minLength: [4, "Name Should Have More Than 4 Charecters"]
  },
  email: {
    type: String,
    required: [true, "Please Enter Your Email"],
    unique: [true, "Email Already Exists"],
    validate: [validator.isEmail, "Please Enter A Correct Email"]
  },
  password: {
    type: String,
    required: [true, "Please Enter Your Password"],
    minLength: [8, "Password Should Be Greater Then 8 Charecters"],
    select: false
  },
  avatar: {
    public_id: {
      type: String,
      required: true
    },
    url: {
      type: String,
      required: true
    }
  },
  role: {
    type: String,
    default: "user"
  },
  resetPasswordToken: String,
  resetPasswordExpire: Date
});

userSchema.pre("save", async function(next){
  if (!this.isModified("password")) {
    next();
  }
  this.password = await bcrypt.hash(this.password, 10);
});

userSchema.methods.getJWTToken = function(){
    return jwt.sign({
        id:this._id
    },JWT_PRIVATE_KEY,
    {
        expiresIn:JWT_EXPIRES
    })
}

userSchema.methods.comparePassword = function(enteredPassword){
    return bcrypt.compare(enteredPassword,this.password)
}

userSchema.methods.getResetPasswordToken = function(){
    const resetToken = crypto.randomBytes(20).toString("hex")
    this.resetPasswordToken = crypto.createHash("sha256").update(resetToken).digest("hex")

    this.resetPasswordExpire = Date.now() + RESET_PASSWORD_EXPIRE * 60 * 1000

    return resetToken
}

const User = mongoose.model("User", userSchema);


