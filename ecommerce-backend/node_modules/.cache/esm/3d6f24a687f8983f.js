let nodeMailer,SMTP_MAIL,SMTP_PASSWORD,SMTP_HOST,SMTP_PORT;_87f‍.x([["sendEmail",()=>sendEmail]]);_87f‍.w("nodemailer",[["default",["nodeMailer"],function(v){nodeMailer=v}]]);_87f‍.w("../../config/appConfig",[["SMTP_MAIL",["SMTP_MAIL"],function(v){SMTP_MAIL=v}],["SMTP_PASSWORD",["SMTP_PASSWORD"],function(v){SMTP_PASSWORD=v}],["SMTP_HOST",["SMTP_HOST"],function(v){SMTP_HOST=v}],["SMTP_PORT",["SMTP_PORT"],function(v){SMTP_PORT=v}]]);







const sendEmail = async (options) => {
  const transporter = nodeMailer.createTransport({
    host: SMTP_HOST,
    port: SMTP_PORT,
    auth: {
      user: SMTP_MAIL,
      pass: SMTP_PASSWORD
    }
  });

  const mailOptions = {
    from: "",
    to: options.email,
    subject: options.subject,
    text: options.Message
  };
  transporter.sendMail(mailOptions);
};


